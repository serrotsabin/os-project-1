Calculations:
awk -F, 'NR>1 {sum+=$4; count++} END {print "Mean:", sum/count}' appendix_latency_raw.txt

awk -F, 'NR>1 {if(min==""||$4<min)min=$4; if($4>max)max=$4} END {print "Min:",min,"Max:",max}' appendix_latency_raw.txt

Build & Run:
clang minimal_pty.c -o minimal_pty
./minimal_pty

System Info:
cd appendix
uname -a > system_info.txt
bash --version >> system_info.txt
gcc --version >> system_info.txt
echo "Date: $(date)" >> system_info.txt
cat system_info.txt 